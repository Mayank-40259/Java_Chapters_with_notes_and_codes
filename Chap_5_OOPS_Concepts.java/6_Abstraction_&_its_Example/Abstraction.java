public class Abstraction {

    //Lecture 06). :- Abstraction.
    //Abstraction : in intellij idea Abstraction package.
    // in Interface.java --> Inheritance_&_its_Types
    
    
    //Access modifiers : public, protected(abstraction lec.), private & default.(no keyword --> anyone not use(rest of three-->public,private,protected) so it will run).
    //it is the keyword in java to decide visibility of class,method,fields..etc. to accessible the class.
    //1).Public : access from anywhere in same or different classes of different and same packages.student,testclass
    //2).private : access in classes in same class.intej idea--> principle,test,utits
    //3).default (no keyword): only two access specifiers used whose default (no write any keyword accessspecifiers) and public. check.it will call in same packages. public or private hata do toh default ho jaha hai ya public laga do toh bhi default ho jata hai.
    //4).protected : access in same package and different package with inheritance.


}
